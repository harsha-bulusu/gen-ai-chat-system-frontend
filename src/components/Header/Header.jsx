import React from 'react';
import { HeaderContainer } from './Header.styled';

export default function Header () {
    return (
        <HeaderContainer>
            Header
        </HeaderContainer>
    )
}